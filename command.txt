#Localhost (dev)
docker compose -f docker-compose.dev.yml --env-file .env up -d
#Multi-server (prod)
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
#On Server A (infra + microservice)
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d
#On Server B (main app):
docker compose -f docker-compose.prod.yml --env-file .env.prod up -d

#Login dockerHub
sudo docker login
#it's still tagged in compose file
#Push in dockerHub
sudo docker push hiiico/vacation_planning-urlaubsplanung